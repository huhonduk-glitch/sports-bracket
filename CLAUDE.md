# CLAUDE.md — 이 저장소에서 작업할 때 반드시 지킬 규칙

## 이 프로젝트가 무엇인가

한국 고등학교 **체육대회용 대진표 자동 생성기**입니다.
사용자는 **코딩을 전혀 모르는 체육 교사**이며, 이 앱을 **실제 학교 현장에서 사용 중**입니다.
따라서 "잘 돌아가던 게 갑자기 안 되는 것"이 가장 큰 사고입니다. 새 기능보다 **안정성과 기존 동작 보존**이 항상 우선입니다.

---

## 절대 규칙 (예외 없음)

### 1. `index.html` 파일 하나 구조를 유지한다
- **파일을 절대 쪼개지 마세요.** JS/CSS를 별도 파일로 분리하지 않습니다.
- 사용자는 이 HTML 파일 하나를 다운로드해서 **더블클릭으로 실행**합니다. 빌드 도구, 번들러, npm, 서버가 전혀 없습니다.
- `file://` 환경에서 동작해야 하므로 ES 모듈(`import`/`export`), fetch로 로컬 파일 읽기 등은 쓸 수 없습니다.

### 2. 기존 기능은 절대 삭제하지 않는다
- 어떤 기능도 "안 쓸 것 같아서", "정리하는 김에" 제거하지 않습니다.
- 기능을 옮기거나 이름을 바꾸는 것도 사전 확인 없이는 하지 않습니다.
- **예외**: 아무 데서도 호출되지 않는 것이 코드로 증명된 죽은 코드는 제거할 수 있으나, 제거 전에 "무엇이, 왜 죽은 코드인지"를 근거와 함께 보고합니다.

### 3. 외부 라이브러리 추가는 먼저 물어본다
- CDN 링크, npm 패키지, 폰트, 아이콘 세트 등 **어떤 외부 의존성도 추가 전에 사용자에게 묻습니다.**
- 이유: 학교망에서 차단될 수 있고, 오프라인(체육관·운동장)에서 동작해야 합니다.
- 현재 이미 들어 있는 외부 의존성은 2개뿐입니다 — `html2canvas`(이미지 저장), `qrcode`(QR 생성). 둘 다 필요할 때만 동적으로 불러오며, 실패해도 앱의 나머지는 정상 동작해야 합니다.

### 4. 모든 UI 텍스트는 한국어
- 버튼, 라벨, 안내문, 오류 메시지, 확인창, 툴팁 — 화면에 보이는 모든 글자는 한국어입니다.
- 코드 주석도 한국어를 권장합니다(사용자가 직접 읽을 수 있어야 함).
- 전문 용어는 풀어서 씁니다. 예: "부전승(첫 경기 없이 다음 라운드 직행)".

### 5. 작업 후 "무엇을 바꿨는지"를 비개발자가 이해할 수 있게 요약한다
- 함수 이름, 변수명, 줄 번호 위주의 설명은 금지입니다.
- **"어느 화면의 무엇이, 어떻게 달라지는가"** 로 설명합니다.
  - 나쁜 예: "`generateTournament()`의 슬롯 배분 로직을 재귀 시드 방식으로 교체함"
  - 좋은 예: "6팀·10팀처럼 팀 수가 딱 떨어지지 않을 때 대진표 중간이 `?`로 막히던 문제를 고쳤습니다. 이제 부전승이 여러 팀에 골고루 나뉩니다. 대회설정 화면은 그대로입니다."
- 사용자가 **브라우저에서 직접 확인할 방법**을 함께 알려줍니다.

### 6. `DIAGNOSIS.md`의 기능 목록을 기준으로 기능 누락을 확인한다
- `DIAGNOSIS.md`의 **"1. 현재 있는 기능 전체 목록"** 이 이 앱의 공식 기능 명세(baseline)입니다.
- 코드를 수정할 때마다 **작업 후 이 목록과 대조**해서 사라지거나 망가진 기능이 없는지 확인하고, 확인했다는 사실을 보고에 포함합니다.
- 기능을 새로 추가했다면 `DIAGNOSIS.md`의 기능 목록에도 추가합니다.

---

## 작업 방식

### 브랜치와 커밋
- 작업은 항상 지정된 작업 브랜치에서 합니다. `main`에 직접 커밋하지 않습니다.
- 커밋 메시지는 한국어로, 무엇이 바뀌었는지 알 수 있게 씁니다.

### 변경 규모
- **한 번에 하나씩** 고칩니다. 여러 버그를 한 커밋에 섞지 않습니다.
- 사용자가 "확인받고 다음으로 넘어가라"고 하면 반드시 멈추고 확인을 받습니다.

### 애매할 때
- 판단이 갈리는 지점(어느 동작이 맞는지 불확실할 때)은 **임의로 결정하지 말고 물어봅니다.**
- 특히 "기존 동작이 버그인지, 의도된 동작인지" 불확실하면 반드시 확인합니다.

### 테스트
- `TESTS.md`에 브라우저에서 직접 눌러 확인하는 점검 목록이 있습니다.
- 코드를 고친 뒤에는 **`TESTS.md`의 어느 항목이 영향을 받는지** 짚어서 알려줍니다.
- 대진표 생성 로직을 건드렸다면 팀 수 3·4·5·6·8·10·12·16개를 **반드시** 검증합니다.

---

## 코드에 대해 알아야 할 것

### 구조
- `index.html` 한 파일 안에 `<style>`, HTML, `<script>`가 모두 들어 있습니다.
- 상태는 전역 변수 `state` 객체 하나에 모입니다(팀, 경기, 일정, 학생, 기록 등).
- 저장은 브라우저 `localStorage` 한 곳입니다. 키: `bracketPro_projects`, `bracketPro_active`, `bracketPro_presets`, `bracketPro_darkMode`.
- 화면 갱신은 "문자열로 HTML을 만들어 `innerHTML`에 넣는" 방식입니다. 프레임워크가 없습니다.

### 조심할 지점
- **함수 중복 정의**: 과거에 파일 뒤쪽 "패치" 스크립트가 앞쪽 함수를 덮어쓰는 구조였습니다(1단계에서 정리됨). 앞으로 **같은 이름의 함수를 두 번 정의하지 마세요.** 고칠 게 있으면 원래 자리에서 고칩니다.
- **날짜 계산**: `new Date('2026-04-20T00:00:00')` 후 `.toISOString()`을 쓰면 한국 시간에서 **하루가 밀립니다.** 날짜를 `YYYY-MM-DD` 문자열로 만들 때는 현지 시각 기준 함수를 쓰세요.
- **이름을 화면에 그릴 때**: 학생·팀 이름은 반드시 HTML 이스케이프(`escHtml`)를 거칩니다. `onclick="...('${이름}')"` 형태로 이름을 직접 끼워 넣지 마세요.
- **팀 식별**: 팀을 이름 문자열로 식별하는 코드가 많습니다. 이름에 하이픈(`-`)이 들어가면 리그 결과 키가 깨질 수 있습니다("1-3반"은 학교에서 흔한 팀명입니다).
- **개인정보**: 학생 이름·성별·실력등급이 저장됩니다. 로그로 출력하거나 외부로 전송하는 코드를 절대 넣지 마세요.

### 사용 환경
- 주 브라우저: 크롬/엣지(PC), 사파리(아이폰), 크롬(안드로이드).
- 사용 장소: 교무실 PC, 체육관·운동장에서의 스마트폰. **인터넷이 없을 수 있습니다.**
- 인쇄(PDF 저장) 사용 빈도가 높습니다. 인쇄 레이아웃을 깨뜨리지 마세요.
