# 대진표 자동 생성기 Pro v4 — 전체 진단서

> 분석 대상: `index.html` (단일 파일, 4,198줄)
> 분석 시점: 2026-08-12 (이후 전 항목 수정 완료)
> 이 문서는 코드를 처음부터 끝까지 읽고, 의심스러운 부분은 실제로 실행해서 확인한 결과입니다.
> **이 문서의 "1. 현재 있는 기능 전체 목록"은 앞으로 모든 작업의 기준(baseline)입니다. 기능을 수정할 때마다 이 목록과 대조해 누락이 없는지 확인해야 합니다.**

---

# 1. 현재 있는 기능 전체 목록

## 상단 프로젝트 바 (항상 보임)
- 여러 대회를 동시에 보관하고 드롭다운으로 전환 (최근 저장순 정렬, 저장 날짜 표시)
- ➕ 새 대회 만들기 / 🗑️ 현재 대회 삭제
- 📤 JSON 파일로 내보내기(백업·공유) / 📥 JSON 불러오기
- 자동 저장 표시등, 현재 대회명 표시

## 👥 팀편성 탭
- **학급 프리셋**: 명단 저장·불러오기·삭제, 같은 이름이면 덮어쓰기 확인, 인원/남녀 수/저장일 표시
- **학생 등록 3가지 방식**
  - 직접 입력 (번호·이름·성별·수준, Enter로 연속 추가, 중복 이름 차단)
  - 일괄 입력 (한 줄 한 명, `번호 이름 성별 수준`, 남/여·M/F, 상중하·A/B/C 모두 인식, 항목 생략 가능)
  - 파일 업로드 (CSV/TXT/TSV, 클릭 또는 드래그&드롭, 구분자 자동 감지, 머리글 자동 인식, **열 매핑 화면**에서 어느 열이 번호/이름/성별/수준인지 지정 + 색상 미리보기, 3열 이하면 자동 인식)
- **명단 일괄 편집**: 자동 번호 매기기, 전체 남/여 지정, 전체 상/중/하 지정, 전체 삭제
- **명단 표**: 표 안에서 바로 성별·수준 변경, 한 명씩 삭제, 실시간 통계(총원/남·여·미지정/상·중·하)
- **편성 설정**: 팀당 인원(2~16명), 팀원 표시 방식(이름/번호/번호+이름), 팀 이름 접두사, 성별 균등 배분, 수준 균등 배분, 나머지 인원 처리(기존 팀에 분배 / 소수팀 추가 생성)
- **조건 설정**: 🚫 같은 팀 금지 / ✅ 반드시 같은 팀, 최대 500회 재시도, 실패한 조건은 경고로 알려줌
- **자동 편성 알고리즘**: 무작위 섞기 → 수준 점수(상3·중2·하1) 기준 배분 → 팀 간 전력 차를 줄이는 200회 교환 최적화
- **결과 화면**: 팀별 카드(팀원 명단, 성별·수준 배지, 남/여/상/중/하 합계), 다시 섞기, 인쇄, **📤 대진표로 보내기**(참가팀으로 자동 등록 + 탭 이동)

## ⚙️ 대회설정 탭
- 대회명 / 종목명
- **대회 방식 3종**
  - 🏆 토너먼트: 3위 결정전 유무, 시드 0/2/4팀, 부전승 팀 자동(랜덤) 또는 **직접 선택**(팀을 눌러 지정, 필요 개수 안내)
  - 📊 리그전: 승·무·패 승점 각각 설정, 1~4라운드(홈&어웨이 등), 총 경기 수 안내
  - 🏅 조별+토너먼트: 조 수(2~16), 조별 진출 팀 수(1~4)
- **스코어 방식**: 단일 점수 / 3세트제 / 5세트제, 방식별 설명 자동 변경
- **득점자 기록 ON/OFF** (배드민턴·탁구처럼 개인 기록이 필요 없는 종목용)
- **참가팀 등록**: 직접 입력 / 자동 생성(개수+접두사) / 일괄 입력, 팀 이름을 클릭해 시드 지정, 🔀 섞기, 전체 삭제, 현재 팀 수 표시
- **일정 설정 3가지 모드**
  - 📅 연속일: 시작일·시작시간부터 매일
  - 🗓️ 요일 선택: 체육 수업 요일(월~금)만 골라서 배정
  - ✏️ 직접 지정: 날짜+시간 슬롯을 하나씩 추가(같은 날 여러 슬롯 가능), 일차별로 묶어서 표시, 개별/날짜별 삭제
  - 각 모드마다 경기 시간(분), 휴식 시간, 하루 최대 경기 수, 코트 수(1~10), 코트 이름(쉼표 구분)
- 🚀 대진표 생성

## 🏅 대진표 탭
- **토너먼트 브라켓**: 라운드 이름 자동(16강·8강·준결승·결승), 우승 칸, 3위 결정전 별도 표시, **라운드별 일정 요약 바**(날짜·시간대·경기 수)
- **경기 카드**: 경기 번호, 상태(대기/종료/BYE), 승자 초록 강조·패자 취소선, 세트별 점수 요약, 날짜·시간·코트 표시, 클릭하면 점수 입력
- **점수 입력 창**: 단일 점수 또는 세트별 점수, 세트 스코어 실시간 합계, 토너먼트 무승부 방지
- 승자 자동으로 다음 라운드 배치, 3위전 진출자(준결승 패자) 자동 배정
- **리그전 화면**: 팀 상호 대전표(격자), 순위표(승점→득실→득점 순 정렬, 1·2·3위 메달 강조), 전체 경기 목록, 라운드별 필터
- **조별 화면**: 조별 순위표(진출권 초록 표시), 조별 경기 목록, 진출팀 태그(A1·B2 등), **조별리그가 모두 끝나면 본선 토너먼트 자동 생성**(A조 1위 ↔ B조 2위 교차 대진)
- ✏️ **팀 이름·팀원 편집**(팀명 바꾸면 경기·일정·기록·조 편성까지 함께 갱신, 팀원 추가/삭제/성별·수준 변경)
- 🔀 재추첨, ↩️ 결과 초기화, 🖨️ 인쇄, 📸 PNG 이미지 저장, 📱 QR코드 생성·저장

## 📅 일정 탭
- 📋 리스트 뷰 / 📅 달력 뷰 (날짜별 카드, 시간순 정렬, Day 번호)
- 경기별 날짜·시간·코트·점수·완료 여부, ✏️ 개별 일정 편집(날짜/시간/코트)
- **순서 편집 모드**: ↑ ↓ ⤒ ⤓ 버튼, 드래그&드롭, `#경기번호 순 정렬`, **⏱ 현재 순서대로 날짜·시간·코트 재배정**
- 요약 카드(총 경기 수 / 완료 수 / 진행 일수 / 시작~종료일)
- 인쇄

## 📊 스코어보드 탭
- **최대 6코트 동시 운영**, 코트마다 독립 점수판
- 코트별 경기 선택(일정 순서대로 목록, 완료 경기 ✅ 표시)
- ＋/− 점수 입력(애니메이션), 세트제일 때 세트 배지·현재 세트 강조·세트 스코어·`세트 종료 → 다음`
- **타이머**: 5·7·10·15·20·30분 선택, ▶ 시작 / ⏸ 일시정지 / ⏹ 정지, 종료 시 알림
- **경기 이벤트 기록**: 득점자 선택, 🅰️ 도움(득점 후 이어서 입력), 🟨 경고, 🟥 퇴장(사유 입력), 최근 이벤트 로그 표시
- ✅ 결과 확정 → 대진표·순위표·일정에 즉시 반영 → **⭐ MVP 선정** 창

## 🏠 팀현황 탭
- **팀 로스터**: 팀별 선수 명단(번호·이름·성별·수준) + 팀 전적(승·무·패·득·실) + 선수별 득점/도움/경고/퇴장/MVP 배지, ✏️ 팀 편집
- **개인 기록**: 득점 순위(메달 표시), 도움 순위, MVP 횟수 순위, 출장 경기 수
- **경고/퇴장**: 선수별 누적, 경고 3회 누적 시 ⚠️ 주의 표시, 전체 카드 기록 타임라인
- **경기 기록**: 경기별 점수·세트·일정·MVP + 득점/도움/경고/퇴장 타임라인(분 단위 정렬)

## ❓ 사용법 탭
- 5분 퀵스타트 표, 기능별 설명표, 꿀팁 14가지, 문제 해결 표

## 새로 추가된 기능 (2026-08 작업)
- **되돌리기(1단계)**: 전체 삭제·다시 추첨·결과 초기화·팀 편성·일정 미루기 직전으로 복구
- **몰수패(결장) 처리**: 점수 입력 창에서 오지 않은 팀을 몰수패로 기록
- **전광판 모드**: 현재 경기 점수·타이머를 전체화면 큰 글씨로 (체육관 TV·빔프로젝터용)
- **다음 경기 호출판**: 점수 입력 탭 상단에 다음 경기와 이어지는 경기 표시
- **화면 꺼짐 방지**: 경기 중 폰 화면이 꺼지지 않게 (Wake Lock)
- **타이머 소리**: 종료 10초 전 카운트다운 + 종료 알림음 (끄고 켤 수 있음)
- **일정 미루기**: 기준 날짜 이후 경기를 통째로 N일 뒤로
- **시상 명단**: 우승·준우승·3위·득점왕·도움왕·최우수선수를 인쇄용 한 장으로
- **엑셀(CSV) 내보내기**: 팀 순위·개인 기록·경기 결과 (엑셀에서 한글 안 깨짐)
- **종합 순위**: 여러 종목(대회)의 순위를 점수로 환산해 팀별 종합 순위 산출
- **조끼 색 지정**: 팀별 색을 지정하면 대진표·일정·명단에 색 점으로 표시
- **심판·기록원 배정**: 경기별 심판 이름 지정, 일정표·다음 경기 카드에 표시
- **백업 안내**: 백업한 지 오래되면 상단에 알림 배너
- **첫 사용 안내 · 빈 화면 안내**: 아무것도 없을 때 무엇을 해야 하는지 안내
- **단계 진행 표시**: 완료한 단계의 탭 번호가 초록색으로 바뀜
- **인쇄물 공유 안내**: 인쇄물 하단에 학생 확인용 주소 표시
- **브랜드 표시**: 제목을 「대진표 메이커」로 정리하고, DEOK TT 마크를 상단·하단·사용법·시상 명단에 표시
  (외부 이미지 파일이 아니라 HTML 안에 직접 그린 SVG라 오프라인에서도 그대로 보임)

## 전역 기능
- 🌙 **다크모드** (선택 기억됨)
- **인쇄 전용 스타일**(버튼·탭 숨김, 카드 잘림 방지)
- **읽기 전용 모드**: 주소 뒤에 `?view=true`를 붙이면 편집 UI가 모두 숨겨지고 대진표·일정·사용법만 보임 (화면 우상단에 '👀 읽기 전용 모드' 표시)
- 모든 변경 즉시 자동 저장(localStorage), 프리셋은 대회와 별도로 보관

> 💡 **잊고 계셨을 가능성이 큰 기능**: 읽기 전용 모드(`?view=true`), 일정 순서 드래그 편집 + 시간 재배정, 부전승 팀 직접 선택, 팀 태그를 눌러 시드 지정, 조건 설정(같은 팀 금지/필수), 라운드별 일정 요약 바, 리그 라운드 필터, MVP 집계.

---

# 2. UI가 산만하게 느껴지는 이유

## (1) 정보 위계가 거의 없음 — 가장 큰 원인
- **모든 카드가 똑같은 무게**입니다. 흰 배경 + 같은 테두리 + 같은 그림자 + 같은 크기 제목(h2 1.15rem / h3 1.0rem, 차이가 겨우 0.15rem). "지금 꼭 해야 할 것"과 "가끔 쓰는 것"이 구분이 안 됩니다.
- **탭 7개가 모두 동등**합니다. 실제 작업 순서(팀편성 → 대회설정 → 대진표 → 일정 → 스코어보드)와 참고 자료(사용법), 결과 조회(팀현황)가 한 줄에 섞여 있습니다.
- **가장 안 쓰는 것이 맨 위**: 팀편성 탭 최상단이 '학급 명단 프리셋'입니다. 처음 쓰는 날에는 저장된 게 없는 빈 카드부터 보게 됩니다.
- **대회설정 탭은 카드 5개가 세로로 쭉**(기본설정·대회방식·스코어방식·참가팀·일정) 이어지고, 마지막에야 '대진표 생성' 버튼이 나옵니다. 필수 설정과 선택 설정이 섞여 있어 "어디까지 건드려야 하나"가 안 보입니다.
- **주 버튼이 눈에 안 띔**: 🎲 팀 자동 편성 / 🚀 대진표 생성이 폰트 1.05rem으로 다른 버튼(0.88rem)과 거의 차이가 없고, 긴 스크롤 맨 아래에 있습니다.
- 팀현황 탭(`#tab-dashboard`)만 다른 탭들과 달리 **중앙 정렬 컨테이너 바깥**에 놓여 있어(코드 790줄) 넓은 화면에서 좌우 여백 없이 화면 끝까지 퍼집니다. 다른 탭과 폭이 달라 "다른 앱 같은" 느낌을 줍니다.

## (2) 색이 너무 많고, 같은 색이 서로 다른 뜻으로 쓰임
- 기본 색만 5종(파랑·초록·빨강·주황·회색)인데 여기에 순위 금/은/동, 성별 파랑/분홍, 수준 빨강/주황/초록, 상태 뱃지 노랑/초록이 더해집니다.
- **결정적 문제**: 실력 '상(A)'이 **빨강**입니다. 앱의 다른 곳에서 빨강은 '삭제·위험·퇴장'인데, 잘하는 학생이 빨간 글씨로 나옵니다. '하(C)'는 초록(=성공/완료 색)이고요. 색의 의미가 정반대로 충돌합니다.
- 주황(경고색)이 📥 불러오기, 🔀 섞기, ⏸ 일시정지, 대기 상태, 경기 시간 표시, '중(B)' 수준에 모두 쓰입니다.
- 한 화면에 표면 종류가 4가지(파란 그라데이션 헤더 / 흰 카드 / 검정 스코어보드 / 파란 그라데이션 푸터)라 시선이 분산됩니다.

## (3) 버튼 배치·개수
- **버튼 무더기 행**: 명단 카드 아래에 `자동 번호매기기 / 전체 남 / 전체 여 / 전체 상 / 전체 중 / 전체 하 / 전체 삭제` 7개가 한 줄에 줄바꿈되며 늘어서 있고, **되돌릴 수 없는 '전체 삭제'가 일상 버튼 바로 옆**에 붙어 있습니다.
- 프로젝트 바의 🗑️(대회 통째 삭제)는 아이콘만 있고, 📤내보내기·📥불러오기와 나란히 붙어 있어 오조작 위험이 큽니다.
- **같은 일을 하는 입구가 너무 많음**: 점수 입력은 대진표 카드 클릭 / 카드의 '점수' 버튼 / 일정 행 클릭 / 리그 대전표 칸 클릭 / 스코어보드 결과 확정 — 5가지 경로가 있습니다. 인쇄 버튼도 3곳에 흩어져 있습니다.
- **아이콘만 있는 버튼**이 많습니다(▶ ⏸ ⏹ ⏱ ⤒ ↑ ↓ ⤓ ✏️ 🗑️). 특히 타이머의 ⏱ 버튼은 "선택한 시간으로 맞추기"인데 추측이 어렵습니다.
- 이모지가 거의 모든 제목·버튼·표 셀에 붙어 있어(한 화면에 30개 이상) 정작 강조가 필요한 곳이 묻힙니다.

## (4) 여백과 글자 크기
- **글씨가 전반적으로 너무 작습니다.** 본문 기준 크기가 지정돼 있지 않고 거의 모든 요소가 0.65~0.9rem(≈10~14px)입니다. 경기 카드 머리글·세트 배지·일정 보조 정보는 0.68rem(≈11px)이라 체육관에서 보기 어렵습니다.
- 반대로 스코어보드 점수는 2.5rem인데 **모바일에서 1.8rem으로 줄어듭니다.** 멀리서 봐야 하는 숫자가 작은 화면에서 더 작아지는 셈입니다.
- 라벨(0.85rem 굵게)과 입력값(0.88rem)이 거의 같은 크기라, 라벨이 뒤로 물러나지 않고 같이 튀어나옵니다.
- 카드 안 여백은 1.2rem인데 카드 사이 간격은 1rem이라 "카드 안"과 "카드 사이"의 구분이 약합니다. 구역을 나누는 구분선이나 소제목 배경이 없어 긴 설정 화면이 한 덩어리로 보입니다.
- 회색 보조 텍스트(#64748b)를 0.72rem에 쓴 곳이 많아 밝은 곳에서 잘 안 보입니다.
- 참고로 `style="flex:2"`처럼 폭을 넓히려고 넣은 지정이 몇 군데 있는데(예: 프리셋 이름 칸), 실제로는 격자 레이아웃이라 **아무 효과가 없습니다.** 그래서 입력칸 폭이 의도와 다르게 균일합니다.

## (5) 다크모드
- 다크모드가 `!important` 강제 지정 250줄로 덧칠돼 있고, 심지어 `[style*="background:#f8fafc"]`처럼 **"인라인 색상 문자열이 일치하면 덮어쓰기"** 방식이 쓰였습니다. 앞으로 색을 하나라도 새로 쓰면 그때마다 예외 규칙을 또 추가해야 하고, 어딘가는 흰 배경에 흰 글씨로 남을 위험이 계속 있습니다.

---

# 3. 코드상의 버그 / 위험한 부분

> **⚠️ 이 장(3번)은 최초 진단 당시의 기록입니다. 아래 ①~㉒는 모두 수정 완료되었습니다.**
> 각 항목은 "무엇이 왜 문제였는지"를 남겨 두기 위해 원문 그대로 보존합니다.
> 현재 상태는 맨 아래 **6. 작업 진행 상황**을 보세요.


## 🔴 심각 — 실제 대회 중 사고가 날 수 있는 것

### ① 부전승 '자동(랜덤)'일 때 대진표가 중간에 막힙니다 — **가장 심각**
팀 수가 2의 거듭제곱(4·8·16)이 아니고 부전승이 2팀 이상 필요할 때, 빈 자리가 **한쪽 끝에 몰려서** 아무도 없는 "BYE vs BYE" 경기가 생깁니다. 그 자리의 승자가 영원히 `?`로 남아, 그와 붙어야 할 팀이 **다음 라운드로 올라가지 못합니다.**

실제로 코드를 그대로 실행해 확인한 결과입니다:

| 팀 수 | 결과 |
|---|---|
| 4, 8, 16팀 | 정상 |
| 3, 7, 15팀 | 정상 (부전승 1팀) |
| **5, 6, 9, 10, 11, 12, 13, 14팀** | **깨짐** |

예) 6팀 → 1라운드가 `1/2`, `3/4`, `5/6`, `BYE/BYE`로 만들어집니다. 부전승을 받아야 할 2팀이 아무도 없고, 대신 빈 경기가 하나 생겨서 준결승 한 자리가 `?`로 고정됩니다. 10팀은 8강 자리 **3개**가 `?`가 됩니다.

> **지금 당장의 회피법**: 대회설정 → 부전승 팀 지정 → **직접 선택**으로 바꾸고 필요한 수만큼 팀을 고르면 정상 배치됩니다(검증 완료). 시드 2/4를 켜도 우회됩니다. 다만 기본값이 '자동(랜덤)'이라 모르고 쓰면 그대로 당합니다.

### ② 일정 날짜가 하루 앞당겨지는 버그 (시간대 처리)
일정 탭의 **"⏱ 현재 순서대로 시간 재배정"**을 누르면, 시작일을 4/20으로 설정해도 **4/19부터** 배정됩니다. 한국 시간(UTC+9)에서 날짜를 세계표준시로 변환하며 하루가 밀리는 전형적인 실수입니다. 실행해서 확인했습니다.

같은 원인으로 **조별리그 → 본선 일정**도 "조별 마지막 날 다음 날"로 계산했는데 결과는 **조별 마지막 날과 같은 날**로 잡힙니다.

처음 '대진표 생성'으로 만드는 일정은 다른 방식(`new Date('2026-04-20')`)이라 정상입니다. 즉 **같은 앱 안에 날짜 계산 방식이 두 가지 섞여 있고, 한쪽만 틀렸습니다.**

### ③ 토너먼트 결과를 나중에 고치면 뒷 라운드가 어긋납니다
1라운드 점수를 잘못 넣고 2라운드까지 진행한 뒤 1라운드를 수정하면, 2라운드의 **팀 이름만** 바뀌고 이미 입력된 2라운드 점수·승자·3라운드 진출자는 그대로 남습니다. "A팀이 이겼다고 되어 있는데 A팀은 사라진" 모순 상태가 됩니다. 결과 수정 시 하위 라운드를 초기화하는 처리가 없습니다.

### ④ 스코어보드 상태가 저장되지 않습니다
- 진행 중인 점수·세트·타이머는 브라우저 저장소에 **저장되지 않습니다.** 경기 중 새로고침하거나 실수로 탭을 닫으면 '결과 확정' 전 점수는 전부 사라집니다.
- 스코어보드 탭을 벗어났다 돌아오면 화면이 새로 그려지면서 **선택했던 경기 표시가 사라지고 팀 이름이 '팀 A/팀 B'로 돌아가는데, 내부적으로는 이전 경기가 선택된 상태로 남습니다.** 이 상태에서 +/-를 누르고 '결과 확정'하면 엉뚱한 경기에 점수가 들어갈 수 있습니다.
- 같은 이유로 **탭을 옮겼다 오면 타이머 남은 시간이 초기값으로 리셋**됩니다(타이머는 계속 도는 채로).
- 다른 대회(프로젝트)를 불러와도 스코어보드 상태가 초기화되지 않아, 이전 대회의 경기 번호를 그대로 들고 있습니다.

### ⑤ 저장 실패를 알려주지 않습니다
브라우저 저장소가 꽉 차거나(사진·학생 수가 많은 대회 여러 개), 시크릿 모드/저장 차단 상황에서 저장이 실패해도 **오류 처리가 전혀 없습니다.** 화면의 표시등은 항상 "✅ 저장됨"으로만 나옵니다(실제로 '저장 안 됨' 빨간 표시는 스타일만 있고 한 번도 쓰이지 않습니다). 즉 **저장이 안 되고 있는데 저장된 줄 알고 계속 쓰게 됩니다.**

### ⑥ 대회를 삭제한 뒤에는 작업이 저장되지 않습니다
🗑️로 대회를 지워도 화면에는 팀·대진표가 그대로 남아 있습니다. 그런데 내부적으로 "현재 대회 없음" 상태가 되어, 그 뒤에 하는 모든 수정이 **조용히 저장되지 않습니다.**

## 🟠 중간 — 특정 상황에서 데이터가 틀어짐

### ⑦ 팀 이름에 하이픈(`-`)이 있으면 리그 결과가 깨질 수 있음
리그 경기 결과를 `팀이름-팀이름-R1` 형태의 문자열로 저장합니다. **"1-3반", "2-5반"처럼 하이픈이 들어간 팀명**을 쓰면 이 문자열을 다시 쪼갤 때 잘못 해석되어, 팀명을 수정할 때 결과가 엉뚱한 경기에 붙거나 사라질 수 있습니다. 학교에서 아주 흔한 팀명이라 실제 위험이 있습니다.

### ⑧ 팀 이름 중복 검사가 편집에는 없음
새 팀 추가 시에는 중복을 막지만, ✏️ 편집으로 팀 이름을 **이미 있는 다른 팀 이름과 같게 바꿀 수 있습니다.** 그러면 두 팀의 경기·결과·기록이 뒤섞입니다.

### ⑨ 동명이인 처리 불가
개인 기록(득점·도움·경고·MVP)이 **이름 문자열만으로** 집계됩니다. 같은 반에 '김민수'가 둘이면 기록이 한 명으로 합쳐집니다. 직접 입력은 동명이인을 막지만, **일괄 입력·파일 업로드는 중복 이름을 그대로 등록**하므로 실제로 발생할 수 있습니다.

### ⑩ 일괄 입력·파일 업로드가 기존 명단을 통째로 지웁니다
'일괄 등록'과 파일 업로드는 기존 명단에 추가하는 게 아니라 **전부 교체**합니다(경고 없음). 참가팀의 '일괄 입력'·'자동 생성'도 마찬가지로 기존 팀을 지웁니다. 반을 나눠 두 번에 걸쳐 입력하면 앞의 것이 날아갑니다.

### ⑪ 엑셀 CSV 한글 깨짐 가능성이 큼
파일을 **UTF-8로만** 읽습니다. 한국 엑셀에서 "CSV(쉼표로 분리)"로 저장하면 보통 CP949(euc-kr)라 **이름이 전부 깨져서** 들어옵니다. 또 UTF-8이라도 엑셀이 붙이는 보이지 않는 표식(BOM) 때문에 첫 줄의 '번호'가 머리글로 인식되지 않고 **학생 한 명("이름"이라는 이름)으로 등록**될 수 있습니다. 사용법 탭에는 "엑셀 → CSV로 저장 후 업로드"라고 안내돼 있는데, 그 안내대로 하면 깨질 확률이 높습니다.
- 덧붙여 쉼표가 들어간 항목(따옴표로 묶인 CSV 표준)을 처리하지 않아, 값 안에 쉼표가 있으면 열이 밀립니다.

### ⑫ '나머지 인원 처리' 설정이 동작하지 않습니다
"기존 팀에 분배 / 소수팀 추가 생성" 선택 항목이 화면에는 있지만, **코드 어디에서도 이 값을 읽지 않습니다**(전체 파일에서 딱 1번, 화면 정의에만 등장). 항상 "기존 팀에 분배"로만 동작합니다.

### ⑬ 조별리그 일정에서 경기가 아예 누락될 수 있음
조별 일정을 만들 때 "하루 최대 경기 수"를 넘는 경기는 **다음 날로 넘기지 않고 그냥 버립니다**(일정이 만들어지지 않음). 조 수가 하루 최대 경기 수보다 많으면 일부 경기가 일정표에서 사라집니다.

### ⑭ 본선은 한 번 만들어지면 다시 계산되지 않습니다
조별리그가 끝나면 본선 대진이 자동 생성되는데, 그 뒤에 **조별 점수를 수정해서 진출팀이 바뀌어도 본선 대진은 그대로**입니다.

### ⑮ 시드를 켜면 부전승이 시드팀에 가지 않습니다
일반적으로 부전승은 상위 시드가 받아야 하는데, 코드는 시드를 정해진 자리에 먼저 놓고 나머지를 앞에서부터 순서대로 채우므로 **부전승은 뒤쪽 아무 팀이나** 받게 됩니다.

### ⑯ 득점자 선택 창의 버튼이 중복
득점 후 뜨는 '선수 선택' 화면이 **점수 입력 창을 재활용**합니다. 그래서 창 아래에 원래 있던 `취소 / 저장` 버튼이 그대로 남아, 화면에 취소 버튼이 2개, 아무 일도 하지 않는 저장 버튼이 1개 보입니다.
또한 '득점 시 선수 선택 팝업 표시'를 끄면 **경고·퇴장·도움 기록 버튼까지 전부 막힙니다**(누르면 "기능이 꺼져 있습니다" 안내). 체크박스 이름과 실제 동작 범위가 다릅니다.

### ⑰ 사용하지 않는 코드가 남아 있음
별도로 만들어 둔 득점자 선택 팝업(`scorerPopup`)은 이제 **아무 데서도 호출되지 않는 죽은 코드**입니다. 이 경로로 기록되는 이벤트에는 '분'과 '시각' 정보가 빠져 있어, 만약 되살아나면 기록이 `undefined분`으로 표시됩니다.

## 🔵 구조적 위험 (지금 당장 고장은 아니지만 앞으로 위험)

### ⑱ 같은 함수가 최대 2~3벌씩 존재합니다
파일 뒤쪽에 "수정 패치" 스크립트 블록이 4개 붙어 있고, 앞에서 만든 함수를 **같은 이름으로 다시 정의해서 덮어쓰는** 방식입니다. 확인된 것만:

`generateLeague`, `generateGroupStage`, `renderLeague`, `renderLeagueMatchList`, `renderGroupStage`, `generateGroupSchedule`, `mcHTML`, `saveModalScore`, `confirmSBResult`, `saveTeamEdit`, `renderSL`, `renderSC`, `populateSBSelect`, `getSelectedWeekdays`, `toggleWeekday`, `gatherSettings`, `applySettings`, `changeSB`, `sbGoal`, `showScorerPopup`, `sbEvent` — **21개**가 중복 정의돼 있습니다.

실제로 동작하는 건 항상 **맨 마지막 것**입니다. 앞으로 누가(사람이든 AI든) 앞쪽 코드를 고치면 **아무 변화가 없거나, 반대로 뒤쪽만 고쳐서 반쯤 적용되는** 상황이 생깁니다. 지금 코드에서 가장 위험한 구조입니다.

### ⑲ 화면에 이름을 그대로 출력합니다 (이스케이프 없음)
학생·팀 이름을 HTML로 그릴 때 특수문자 처리를 하지 않습니다(`escHtml` 함수를 만들어 놓고 4곳에서만 씁니다). 그래서:
- 이름에 `<`, `>`, `"`, `'`가 들어가면 **화면이 깨지거나 버튼이 동작하지 않습니다.**
- 더 중요한 건, **다른 선생님에게 받은 JSON 파일을 불러올 때** 그 안에 심어진 코드가 내 브라우저에서 실행될 수 있습니다. 지금은 로컬 파일이라 피해가 제한적이지만, "JSON 파일을 주고받아 공유하세요"가 공식 사용법인 만큼 **믿을 수 있는 사람이 준 파일만 불러오시는 게 좋습니다.**

### ⑳ 개인정보 측면
학생 **이름·성별·실력 등급(상/중/하)**이 브라우저에 평문으로 저장되고, 내보내기 JSON에도 그대로 담깁니다. 특히 실력 등급은 민감할 수 있는 평가 정보입니다.
- 교무실·체육관 **공용 PC에서 쓰면 다음 사용자에게 그대로 남습니다**(브라우저 저장소에 계속 보관).
- JSON 파일을 메신저로 주고받으면 학생 명단이 그대로 유통됩니다.
- 학교 개인정보 지침에 따라 사용 후 '🗑️ 삭제' + 브라우저 데이터 정리를 권장합니다.

### ㉑ 인터넷이 필요한 기능 2개
📸 이미지 저장, 📱 QR코드는 실행할 때마다 **외부 사이트(cdnjs, jsdelivr)에서 프로그램을 내려받습니다.** 학교망에서 차단되면 동작하지 않고, 무결성 검증(SRI)도 없습니다. 또 파일을 더블클릭으로 여는 방식(`file://`)에서는 **QR코드에 대진표 주소가 아니라 그냥 텍스트("대회명 / 8팀 / 제작자")만 들어갑니다.** 학생이 스캔해도 대진표를 볼 수 없습니다 — 기대와 다르게 동작하는 부분입니다.

### ㉒ 기타 자잘한 것
- '↩️ 초기화'는 결과만 지우는 게 아니라 **대진을 다시 추첨**합니다(토너먼트의 경우 대진표가 바뀝니다).
- '새 대회'를 만들어도 '직접 지정' 일정의 날짜 목록은 **이전 대회 것이 그대로 남습니다.**
- 인쇄 시 대진표가 가로로 넓으면(8강 이상) **오른쪽이 잘립니다.** 가로 스크롤 영역이라 종이 폭을 넘는 부분은 인쇄되지 않습니다.
- 앱을 다른 폴더로 옮기거나 다른 브라우저로 열면 저장 데이터가 안 보일 수 있습니다(브라우저별·경로별로 저장소가 분리됨).
- 알림창(`alert`/`confirm`/`prompt`)을 매우 많이 씁니다. 브라우저가 "이 페이지에서 추가 대화상자 표시 안 함"을 띄운 뒤 사용자가 체크하면, 이후 **삭제 확인·MVP 선정·팀 선택이 전부 조용히 실패**합니다.

---

# 4. 모바일에서 문제가 될 부분

경기 중 스마트폰으로 점수를 입력하는 상황을 가정하면 아래가 실질적인 문제입니다.

### 조작 (가장 큰 문제)
1. **점수 +/- 버튼이 30×30px입니다.** 권장 터치 크기(약 44px)보다 훨씬 작습니다. 뛰면서·장갑 끼고·급할 때 누르기 어렵고, 옆의 −를 잘못 누르기 쉽습니다. **경기 운영 중 가장 많이 누르는 버튼**이라 체감이 큽니다.
2. ✏️ 편집 버튼, 팀 태그의 ×(삭제), 작은 버튼(btn-sm) 모두 터치 목표가 작습니다. 특히 **× 삭제가 손가락에 비해 너무 작아 오터치 위험**이 있습니다.
3. **일정 순서 드래그&드롭이 모바일에서 동작하지 않습니다.** PC용 방식(HTML5 drag)이라 터치로는 끌리지 않습니다. 그런데 화면 안내에는 "↑↓ 버튼 또는 드래그로 바꿀 수 있습니다"라고 적혀 있어 혼란을 줍니다(↑↓ 버튼은 동작합니다).

### 화면·글씨
4. **아이폰 사파리에서 입력칸을 누를 때마다 화면이 확대됩니다.** 입력 글자 크기가 0.88rem(≈14px)인데, iOS는 16px 미만이면 자동 확대하고 자동으로 되돌리지 않습니다. 명단을 여러 명 입력할 때 계속 확대·축소를 반복하게 됩니다.
5. **멀리서 봐야 할 점수판이 모바일에서 더 작아집니다** (2.5rem → 1.8rem, 타이머도 동일). 폰을 세워두고 심판대로 쓰기엔 작습니다.
6. 표가 많고 폭이 넓습니다. 리그 대전표는 12팀이면 열이 14개, 학생 명단은 **세로 스크롤 영역(최대 400px) 안에 가로 스크롤**이 또 들어 있어 손가락 스크롤이 자주 엇나갑니다.
7. 대진표는 라운드마다 최소 190px이라 8강이면 약 760px — 가로 스크롤이 필수인데, 스크롤 가능하다는 표시가 없어 오른쪽에 내용이 더 있는 걸 모를 수 있습니다.
8. **탭 7개**가 좁은 화면에서 가로 스크롤로 밀리는데, 스크롤 힌트가 없어 '사용법' 탭 등이 있는 걸 모를 수 있습니다. 게다가 탭 바가 화면 상단에 고정(sticky)이라 세로 공간도 계속 먹습니다.
9. 일정 목록이 모바일에서는 2칸 격자로 바뀌면서 날짜·시간·코트·상태가 아래로 어색하게 접힙니다.
10. **오른쪽 아래 🌙 다크모드 버튼이 화면에 고정**되어 있어, 그 자리에 오는 버튼(예: 스코어보드 하단, 카드 마지막 버튼)을 가립니다. 아이폰 하단 홈 바 영역과도 겹칩니다.

### 데이터 보존
11. **아이폰 사파리는 7일 이상 앱을 안 열면 저장 데이터를 자동 삭제할 수 있습니다.** 안드로이드에서도 "브라우저 데이터 삭제" 한 번이면 전부 사라집니다. 모바일에서 쓴다면 **📤 내보내기 백업이 PC보다 훨씬 더 중요합니다.**
12. 모바일에서는 파일 다운로드/업로드(JSON, CSV) 경로가 기기마다 달라 백업·복원 흐름이 번거롭습니다.
13. 반복되는 알림창(경기마다 결과 확정 → 알림 → MVP 선정 프롬프트)이 모바일에서 특히 거슬리고, 앞서 말한 "대화상자 차단"에 걸리기 쉽습니다.

---

# 5. 정리 — 우선순위

| 순위 | 항목 | 왜 |
|---|---|---|
| 1 | 랜덤 부전승 대진표 깨짐 (5·6·9~14팀) | 대회 당일 진행이 막힘. **당장은 '부전승 직접 선택'으로 회피 가능** |
| 2 | 스코어보드 상태 미저장 + 탭 전환 시 초기화 | 경기 중 실제로 점수를 잃음 |
| 3 | 저장 실패를 알리지 않음 | 데이터가 통째로 날아갈 수 있음 |
| 4 | 날짜 하루 밀림(재배정·본선 일정) | 학생·동료 교사에게 잘못된 일정 배포 |
| 5 | 함수 21개 중복 정의 | 앞으로 어떤 수정도 위험해지는 구조. 손대기 전에 정리 필요 |
| 6 | 모바일 터치 크기·iOS 확대·드래그 미동작 | 현장 사용성 |
| 7 | UI 위계·색상 정리 | 산만함의 근본 원인 |

---

# 6. 작업 진행 상황

| 단계 | 내용 | 상태 |
|---|---|---|
| 1단계 | 중복 함수 21개 정리 + 죽은 코드(⑰) 제거 — 기능 변화 0 | ✅ 완료 |
| 2단계 | 🔴 심각 항목 ① ② ③ ④ ⑤ ⑥ (⑮도 함께 해결) | ✅ 완료 |
| UI 1차 | 탭 단계화 · 하단 고정 버튼 · 필수/선택 분리 · 색·글자 위계 · 모바일 | ✅ 완료 |
| UI 2차 | 첫 사용 안내 · 빈 화면 안내 · 단계 진행 표시 · 점수 버튼 확대 | ✅ 완료 |
| 기능 추가 | 되돌리기 · 몰수패 · 전광판 · 다음경기 · 화면꺼짐방지 · 소리 · 일정미루기 · 시상명단 · CSV · 종합순위 · 조끼색 · 심판 · 백업알림 · 인쇄 공유안내 | ✅ 완료 |
| 3단계 | 🟠 중간 항목 ⑦ ⑧ ⑨ ⑩ ⑪ ⑫ ⑬ ⑭ ⑯ | ✅ 완료 |
| 5단계 | 다크모드 CSS 변수화 + ⑲ 이스케이프·불러오기 검증 + ⑳ 개인정보 | ✅ 완료 |
| 마무리 | ㉑ 오프라인 안내, ㉒ 결과 초기화 분리·인쇄 잘림 | ✅ 완료 |

**남은 것 없음.** 진단서에 적힌 모든 항목이 처리되었습니다.

## 3~5단계에서 고친 내용 요약
- ⑦ 팀 이름에 `-`(1-3반)가 있어도 리그 결과가 깨지지 않도록 결과 키 방식 변경 + 기존 데이터 자동 이전
- ⑧ 팀 이름을 이미 있는 이름으로 바꾸려 하면 막음
- ⑨ 같은 이름이 있으면 자동으로 구분 번호를 붙이고, 개인 기록도 팀+이름으로 구분
- ⑩ 일괄 입력·파일 업로드 시 "뒤에 추가 / 지우고 교체"를 선택
- ⑪ 한국 엑셀 CSV(CP949) 자동 인식, BOM 처리, 따옴표 안 쉼표 처리, 인식한 형식 화면 표시
- ⑫ '나머지 인원 처리 → 소수팀 추가 생성'이 실제로 동작
- ⑬ 조별 일정에서 하루 최대를 넘으면 버리지 않고 다음 날로 넘김
- ⑭ 조별 결과가 바뀌면 본선 대진을 다시 만들지 물어봄
- ⑯ 득점자 선택 창의 중복 버튼 제거, 경고·퇴장은 설정과 무관하게 기록 가능
- ⑲ 이름·팀명을 화면에 그릴 때 전부 이스케이프, 백업 파일 불러올 때 형식 검증
- ⑳ 이름 빼고 내보내기, 사용 후 데이터 전체 삭제 버튼, 사용법 탭에 개인정보 안내
- 다크모드를 색 토큰 방식으로 바꿔 "인라인 색 문자열 일치" 규칙을 모두 제거
- ㉒ '결과 초기화'가 대진을 다시 추첨하지 않고 결과만 지우도록 분리, 인쇄 시 대진표 잘림 방지

## 2단계에서 실제로 확인한 것
- 부전승: **3팀~32팀 전 구간을 각 200회 무작위 반복** 검증 → 전부 정상
  (양쪽 다 빈 경기 0건, 막히는 자리 0건, 우승팀까지 진행 확인)
- 시드 지정 시 상위 시드가 부전승을 받음, '직접 선택' 시 고른 팀이 정확히 부전승
- 토너먼트·리그전·조별 3가지 형식 모두 브라우저 로드 오류 0건
- 새 기능 18개를 3가지 형식에서 각각 호출 → 오류 0건
